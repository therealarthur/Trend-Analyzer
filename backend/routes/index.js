// Loads app server using express.
const express = require("express");
const morgan = require("morgan");
const router = express.Router();
const db = require("../db");

// Will set up all routes in here

module.exports = router;
